[phases.setup]
nixPkgs = [
  "php82",
  "php82Extensions.mbstring",
  "php82Extensions.curl",
  "php82Extensions.pdo",
  "php82Extensions.json",
  "php82Extensions.openssl"
]

[phases.install]
cmds = ["echo 'Installing PHP dependencies...'"]

[start]
cmd = "php -S 0.0.0.0:$PORT -t ."
